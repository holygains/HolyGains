# Node modules (if using JS/Node for any parts of the project)
node_modules/

# Environment files (never leak secrets like API keys!)
.env
.env.local
.env.development.local
.env.production.local

# Dependency locks
package-lock.json
yarn.lock
pnpm-lock.yaml

# Compiled output
/dist
/build
/out

# Logs (no one needs to see error logs in the repo)
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Cache and temp files
.cache
.tmp
.DS_Store
*.swp
*.bak
Thumbs.db

# IDE-specific files (VSCode, JetBrains, etc.)
.vscode/
.idea/
*.iml

# Private keys and wallet stuff (super important!)
wallet_private_key.json
keystore/
secrets/
*.pem

# OS-specific files (Windows/Mac leftovers)
*.exe
*.bat
*.dll
*.app
*.ico

# Python stuff (if using any scripts)
__pycache__/
*.py[cod]
*.pyo

# Solidity/Blockchain (if applicable)
*.bin
*.abi
*.sol.js

